export interface ICashier{
    id: number,
    photo: string,
    address: string,
    cp: string,
    location: string,
    latitude: number,
    longitude: number,
    balance: number,
    available: boolean
}